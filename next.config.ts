/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "randomuser.me"], // Add randomuser.me as an allowed domain
  },
};

module.exports = nextConfig;

